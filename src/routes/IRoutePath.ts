export enum IRoutePath {
   HOME = "/",
   TICKET = "/ticket",
   LINEUP = "/line-up",
   TICKETING = "/ticketing",
   EVENTS = "/events",
   LIVEMAP = "/live-map",
   NOTICE = "/notice",
   ADMIN = "/admin",
}
